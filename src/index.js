import VRadioDeselectable from './component.vue'

const install = (Vue) => {
    Vue.component('v-radio-deselectable', VRadioDeselectable)
}

export default {
    install,
}

export {
    VRadioDeselectable,
}
