import {VRadioDeselectable} from '../src/index'

export default {
    // Local component registration
    components: {
        'v-radio-deselectable': VRadioDeselectable,
    },
    data() {
        return {
            model: {},
        }
    },
}
